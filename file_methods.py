#close() close and open a file, no effect if a file is alreday closed
#detach() separate underlying binary buffer from textObase and return it
#fileno() return an interger number(file descriptor) of the file
#flush() flush the write buffer of the file stream
#isatty() Return true if file stream is interactive
#read(n) read most n character from the file. reads till ebnd of file if it is negative or none.
#readable() returns true if the file stream can be read from
#readline(n=-1) ready and return one line from the file. Reads in at most n bytes if specified
#



##########################